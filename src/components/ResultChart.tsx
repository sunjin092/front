import React from 'react';  // `import` 추가로 모듈로 만듦.

const ResultChart: React.FC = () => {
    return (
        <div>
            <h1>결과 차트</h1>
        </div>
    );
};

export default ResultChart;  // `export` 추가
